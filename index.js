const tasks = [];
const input = document.querySelector("#text-input");
const form = document.querySelector("#submit-form")
const p = document.querySelector(".task-name")
form.addEventListener("submit",(e)=>{
    e.preventDefault()
    
})